# 从零开始构建 AI 代理：迭代与链

> [原文链接](https://towardsdatascience.com/ai-agents-from-zero-to-hero-part-2/)

## **<mdspan datatext="el1743017574199" class="mdspan-comment">简介</mdspan>**

在本教程系列的[第一部分](https://towardsdatascience.com/ai-agents-from-zero-to-hero-part-1/)中，我们介绍了**AI 代理**，这是执行任务、做出决策并与他人通信的自主程序。

代理通过工具执行操作。可能发生的情况是，某个工具第一次尝试时不起作用，或者必须按顺序激活多个工具。代理应该能够将任务组织成逻辑顺序，并在动态环境中调整其策略。

简单来说，代理的结构必须稳固，行为必须可靠。最常见的方法是通过：

+   **迭代** - 重复执行某个动作多次，通常在每个周期中都有细微的变化或改进。每次可能涉及代理重新访问某些步骤以细化其输出或达到最佳解决方案。

+   **链** - 一系列按顺序链接的动作。链中的每一步都依赖于前一步，一个动作的输出成为下一个动作的输入。

在这个教程中，我将展示如何为代理使用**迭代和链**。我将提供一些易于在其他类似情况下应用的 Python 代码（只需复制、粘贴、运行），并逐行带注释地解释代码，以便您可以复制此示例（文章末尾有完整代码的链接）。

## **设置**

请参阅[第一部分](https://towardsdatascience.com/ai-agents-from-zero-to-hero-part-1/)了解[***Ollama***](https://ollama.com/)的设置和主要大型语言模型。

```py
import ollama
llm = "qwen2.5" 
```

我们将使用[*YahooFinance*公共 API](https://yfinance-python.org/)和 Python 库（`pip install yfinance==0.2.55`）来下载财务数据。

```py
import yfinance as yf

stock = "MSFT"
yf.Ticker(ticker=stock).history(period='5d') #1d,5d,1mo,3mo,6mo,1y,2y,5y,10y,ytd,max
```

![图片](img/f78d6ac26569e06de1298387249dddd2.png)

让我们将它嵌入到一个工具中。

```py
import matplotlib.pyplot as plt

def get_stock(ticker:str, period:str, col:str):
    data = yf.Ticker(ticker=ticker).history(period=period)
    if len(data) > 0:
        data[col].plot(color="black", legend=True, xlabel='', title=f"{ticker.upper()} ({period})").grid()
        plt.show()
        return 'ok'
    else:
        return 'no'

tool_get_stock = {'type':'function', 'function':{
  'name': 'get_stock',
  'description': 'Download stock data',
  'parameters': {'type': 'object',
                'required': ['ticker','period','col'],
                'properties': {
                    'ticker': {'type':'str', 'description':'the ticker symbol of the stock.'},
                    'period': {'type':'str', 'description':"for 1 month input '1mo', for 6 months input '6mo', for 1 year input '1y'. Use '1y' if not specified."},
                    'col': {'type':'str', 'description':"one of 'Open','High','Low','Close','Volume'. Use 'Close' if not specified."},
}}}}

## test
get_stock(ticker="msft", period="1y", col="Close")
```

![图片](img/ea1f9b6db9a06611d982aad95c41c8b6.png)

此外，以上一篇文章中的代码为参考，我将编写一个通用函数来处理模型响应，例如当代理想要使用工具或它只是返回文本时。

```py
def use_tool(agent_res:dict, dic_tools:dict) -> dict:
    ## use tool
    if "tool_calls" in agent_res["message"].keys():
        for tool in agent_res["message"]["tool_calls"]:
            t_name, t_inputs = tool["function"]["name"], tool["function"]["arguments"]
            if f := dic_tools.get(t_name):
                ### calling tool
                print('🔧 >', f"\x1b[1;31m{t_name} -> Inputs: {t_inputs}\x1b[0m")
                ### tool output
                t_output = f(**tool["function"]["arguments"])
                print(t_output)
                ### final res
                res = t_output
            else:
                print('🤬 >', f"\x1b[1;31m{t_name} -> NotFound\x1b[0m")
    ## don't use tool
    if agent_res['message']['content'] != '':
        res = agent_res["message"]["content"]
        t_name, t_inputs = '', ''
    return {'res':res, 'tool_used':t_name, 'inputs_used':t_inputs}
```

让我们与我们的代理进行一次快速对话。目前，我将使用一个简单的通用提示。

```py
prompt = '''You are a financial analyst, assist the user using your available tools.'''
messages = [{"role":"system", "content":prompt}]
dic_tools = {'get_stock':get_stock}

while True:
    ## user input
    try:
        q = input('🙂 >')
    except EOFError:
        break
    if q == "quit":
        break
    if q.strip() == "":
        continue
    messages.append( {"role":"user", "content":q} )

    ## model
    agent_res = ollama.chat(model=llm, messages=messages,
                            tools=[tool_get_stock])
    dic_res = use_tool(agent_res, dic_tools)
    res, tool_used, inputs_used = dic_res["res"], dic_res["tool_used"], dic_res["inputs_used"]

    ## final response
    print("👽 >", f"\x1b1;30m{res}\x1b[0m")
    messages.append( {"role":"assistant", "content":res} )
```

如您所见，我首先提出了一个“简单”的问题。大型语言模型已经知道微软股票的符号是 MSFT，因此代理能够通过正确的输入激活工具。但如果我们问一些可能不在大型语言模型知识库中的问题呢？

![图片

看起来大型语言模型不知道 Facebook 将其名称更改为 META，因此它使用了错误的输入来使用工具。我将使代理能够通过迭代尝试多次执行动作。

## **迭代**

迭代是指重复一个过程，直到满足某个条件。我们可以让代理尝试特定的次数，但我们需要通过在消息历史中添加详细信息来让代理知道之前的参数不起作用。

```py
    max_i, i = 3, 0
    while res == 'no' and i < max_i:
        comment = f'''I used tool '{tool_used}' with inputs {inputs_used}. But it didn't work, so I must try again with different inputs'''
        messages.append( {"role":"assistant", "content":comment} )
        agent_res = ollama.chat(model=llm, messages=messages,
                                tools=[tool_get_stock])
        dic_res = use_tool(agent_res, dic_tools)
        res, tool_used, inputs_used = dic_res["res"], dic_res["tool_used"], dic_res["inputs_used"]

        i += 1
        if i == max_i:
            res = f'I tried {i} times but something is wrong'

    ## final response
    print("👽 >", f"\x1b1;30m{res}\x1b[0m")
    messages.append( {"role":"assistant", "content":res} )
```

![

代理尝试了 3 次，使用不同的输入，但由于 LLM 知识库中存在差距，它无法找到解决方案。在这种情况下，模型需要人类输入来理解如何使用工具。

接下来，我们将使代理能够自己填补知识差距。

## **链**

链指的是一系列线性动作，其中一步的输出用作下一步的输入。在这个例子中，我将添加另一个代理在第一个工具失败时可以使用的工具。

我们可以使用上一篇文章中提到的网络搜索工具。

```py
from langchain_community.tools import DuckDuckGoSearchResults

def search_web(query:str) -> str:
  return DuckDuckGoSearchResults(backend="news").run(query)

tool_search_web = {'type':'function', 'function':{
  'name': 'search_web',
  'description': 'Search the web',
  'parameters': {'type': 'object',
                'required': ['query'],
                'properties': {
                    'query': {'type':'str', 'description':'the topic or subject to search on the web'},
}}}}

## test
search_web(query="facebook stock")
```

![](img/df35b941be6ad9a440d3a15e4ff1ce1b.png)

到目前为止，我总是使用非常通用的提示词，因为任务相对简单。现在，我想确保代理能够理解如何正确地按顺序使用工具，所以我将编写一个**合适的提示词**。这是提示词应该完成的步骤：

1.  代理的目标

1.  它必须返回的内容（即格式、内容）

1.  可能影响输出的任何相关警告

1.  上下文导出

```py
prompt = '''
[GOAL] You are a financial analyst, assist the user using your available tools.

[RETURN] You must return the stock data that the user asks for.

[WARNINGS] In order to retrieve stock data, you need to know the ticker symbol of the company.

[CONTEXT] First ALWAYS try to use the tool 'get_stock'.
If it doesn't work, you can use the tool 'search_web' and search 'company name stock'.
Get information about the stock and deduct what is the right ticker symbol of the company.
Then, you can use AGAIN the tool 'get_stock' with the ticker you got using the previous tool.
'''
```

我们可以简单地将链添加到我们已有的迭代循环中。这次代理有两个工具，当第一个失败时，模型可以决定是重试还是使用第二个。然后，如果使用第二个工具，**代理必须处理输出并学习**最初失败的第一个工具的正确输入。

```py
    max_i, i = 3, 0
    while res in ['no',''] and i < max_i:
        comment = f'''I used tool '{tool_used}' with inputs {inputs_used}. But it didn't work, so I must try a different way.'''
        messages.append( {"role":"assistant", "content":comment} )
        agent_res = ollama.chat(model=llm, messages=messages,
                                tools=[tool_get_stock, tool_search_web])
        dic_res = use_tool(agent_res, dic_tools)
        res, tool_used, inputs_used = dic_res["res"], dic_res["tool_used"], dic_res["inputs_used"]

        ## chain: output of previous tool = input of next tool
        if tool_used == 'search_web':
            query = q+". You must return just the compay ticker.\nContext: "+res
            llm_res = ollama.generate(model=llm, prompt=query)["response"]
            messages.append( {"role":"user", "content":f"try ticker: {llm_res}"} )

            print("👽 >", f"\x1b[1;30mI can try with {llm_res}\x1b[0m")

            agent_res = ollama.chat(model=llm, messages=messages, tools=[tool_get_stock])
            dic_res = use_tool(agent_res, dic_tools)
            res, tool_used, inputs_used = dic_res["res"], dic_res["tool_used"], dic_res["inputs_used"]
        i += 1        if i == max_i:            res = f'I tried {i} times but something is wrong'

 ## final response    
 print("👽 >", f"\x1b1;30m{res}\x1b[0m")    
 messages.append( {"role":"assistant", "content":res} )
```

![如预期的那样，代理试图使用第一个工具并使用错误的输入，但它没有像以前那样再次尝试相同的动作，而是决定使用第二个工具。通过消耗信息，它应该能够理解解决方案，而无需人类输入。![](img/e18010afb8d0dfa2d8c611f44ec7c468.png)

总结来说，AI 尝试执行一个动作，但由于知识库中的差距而失败。因此，它激活了工具来填补这个差距，并提供了用户请求的输出……这确实是 AI 代理的真正本质。

## **结论**

本文介绍了更多结构化的方法来使代理更加可靠，使用迭代和链。有了这些构建块，您已经准备好开始为不同的用例开发自己的代理。

**敬请期待第三部分**，我们将深入探讨更多高级示例。

本文的完整代码：[**GitHub**](https://github.com/mdipietro09/GenerativeAI/blob/main/Agents_ZeroToHero/notebook.ipynb)

希望您喜欢！欢迎联系我提问、反馈或分享您有趣的项目。

👉 [**让我们连接**](https://maurodp.carrd.co/) 👈

![](img/1e0e1083fd6b193ac7b714448044ddde.png)
