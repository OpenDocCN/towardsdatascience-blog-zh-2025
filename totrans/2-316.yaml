- en: Estimating Product-Level Price Elasticities Using Hierarchical Bayesian
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: 原文：[https://towardsdatascience.com/estimating-product-level-price-elasticities-using-hierarchical-bayesian/](https://towardsdatascience.com/estimating-product-level-price-elasticities-using-hierarchical-bayesian/)
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
- en: In this article, I will introduce you to hierarchical Bayesian (HB) modelling,
    a flexible approach to automatically combine the results of multiple sub-models.
    This method enables estimation of individual-level effects by optimally combining
    information across different groupings of data through Bayesian updating. This
    is particularly valuable when individual units have limited observations but share
    common characteristics/behaviors with other units.
  prefs: []
  type: TYPE_NORMAL
- en: The following sections will introduce the concept, implementation, and alternative
    use cases for this method.
  prefs: []
  type: TYPE_NORMAL
- en: The Problem with Traditional Approaches
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: 'As an application, imagine that we’re a large grocery store trying to maximize
    product-level revenue by setting prices. We would need to estimate the demand
    curve (elasticity) for each product, then optimize some profit maximization function.
    As the first step to this workstream, we would need to estimate the price elasticity
    of demand (how responsive demand is to a 1% change in price) given some longitudinal
    data with $i \in N$ products over $t \in T$ periods. Remember that the price elasticity
    of demand is defined as:'
  prefs: []
  type: TYPE_NORMAL
- en: $$\beta=\frac{\partial \log{\textrm{Units}}_{it}}{\partial \log \textrm{Price}_{it}}$$
  prefs: []
  type: TYPE_NORMAL
- en: 'Assuming no confounders, we can use a log-linear [fixed-effect regression model](https://theeffectbook.net/ch-FixedEffects.html)
    to estimate our parameter of interest:'
  prefs: []
  type: TYPE_NORMAL
- en: $$\log(\textrm{Units}_{it})= \beta \log(\textrm{Price})_{it} +\gamma_{c(i),t}+
    \delta_i+ \epsilon_{it}$$
  prefs: []
  type: TYPE_NORMAL
- en: '<mdspan datatext="el1748030373035" class="mdspan-comment">Where</mdspan> $\gamma_{c(i),t}$
    is a set of category-by-time dummy variables to capture the average demand in
    each category-time period and $\delta_i$ is a set of product dummies to capture
    the time-invariant demand shifter for each product. This “fixed-effect” formulation
    is standard and common in many regression-based models to control for unobserved
    confounders. This (pooled) regression model allows us to recover the average elasticity
    $\beta$ across all $N$ units. This would mean that the store could target an average
    price level across all products in their store to maximize the revenue:'
  prefs: []
  type: TYPE_NORMAL
- en: $$\underset{\textrm{Price}_t}{\max} \;\;\; \textrm{Price}_{t}\cdot\mathbb{E}(\textrm{Quantity}_{t}
    | \textrm{Price}_{t}, \beta)$$
  prefs: []
  type: TYPE_NORMAL
- en: 'If these units have a natural grouping (product categories), we might be able
    to identify the average elasticity of each category by running separate regressions
    (or interacting the price elasticity with the product category) for each category
    using only units from that category. This would mean that the store could target
    average prices in each category to maximize category-specific revenue, such that:'
  prefs: []
  type: TYPE_NORMAL
- en: $$\underset{\textrm{Price}_{c(i),t}}{\max} \;\;\; \textrm{Price}_{c(i),t}\cdot\mathbb{E}(\textrm{Quantity}_{c(i),t}
    | \textrm{Price}_{c(i),t}, \beta_{c(i)})$$
  prefs: []
  type: TYPE_NORMAL
- en: With sufficient data, we could even run separate regressions for each individual
    product to obtain more granular elasticities.
  prefs: []
  type: TYPE_NORMAL
- en: 'However, real-world data often presents challenges: some products have minimal
    price variation, short sales histories, or class imbalance across categories.
    Under these restrictions, running separate regressions to identify product elasticity
    would likely lead to large standard errors and weak identification of $\beta$.
    HB models addresses these issues by allowing us to obtain granular estimates of
    the coefficient of interest by sharing statistical strength both across different
    groupings while preserving heterogeneity. With the HB formulation, it is possible
    to run one single regression (like the pooled case) while still recovering elasticities
    at the product level, allowing for granular optimizations.'
  prefs: []
  type: TYPE_NORMAL
- en: Understanding Hierarchical Bayesian Models
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: 'At its core, HB is about recognizing the natural structure in our data. Rather
    than treating all observations as completely independent (many separate regressions)
    or forcing them to follow identical patterns (one pooled regression), we acknowledge
    that observations can cluster into groups, with products inside each group sharing
    similar patterns. The “hierarchical” aspect refers to how we organize our parameters
    in different levels. In its most basic format, we could have:'
  prefs: []
  type: TYPE_NORMAL
- en: A Global parameter that applies to all data.
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: Group-level parameters that apply to observations within that group.
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: Individual-level parameters that apply to each specific individual.
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: This methodology is flexible enough to add or remove hierarchies as needed,
    depending on the desired level of pooling. For example, if we think there are
    no similarities across categories, we could remove the global parameter. If we
    think that these products have no natural groupings, we could remove the group-level
    parameters. If we only care about the group-level effect, we can remove the individual-level
    parameter and have the group-level coefficients as our most granular parameter.
    If there exists the presence of subgroups nested within the groups, we can add
    another hierarchical layer. The possibilities are endless!
  prefs: []
  type: TYPE_NORMAL
- en: The “Bayesian” aspect refers to how we update our beliefs about these parameters
    based on observed data. We first start with a proposed prior distribution that
    represent our initial belief of these parameters, then update them iteratively
    to recover a posterior distributions that incorporates the information from the
    data. In practice, this means that we use the global-level estimate to inform
    our group-level estimates, and the group-level parameters to inform the unit-level
    parameters. Units with a larger number of observations are allowed to deviate
    more from the group-level means, while units with a limited number of observations
    are pulled closer to the means.
  prefs: []
  type: TYPE_NORMAL
- en: 'Let’s formalize this with our price elasticity example, where we (ideally)
    want to recover the unit-level price elasticity. We estimate:'
  prefs: []
  type: TYPE_NORMAL
- en: $$\log(\textrm{Units}_{it})= \beta_i \log(\textrm{Price})_{it} +\gamma_{c(i),t}
    + \delta_i + \epsilon_{it}$$
  prefs: []
  type: TYPE_NORMAL
- en: 'Where:'
  prefs: []
  type: TYPE_NORMAL
- en: $\beta_i \sim \textrm{Normal}(\beta_{c\left(i\right)},\sigma_i)$
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: $\beta_{c(i)}\sim \textrm{Normal}(\beta_g,\sigma_{c(i)})$
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: $\beta_g\sim \textrm{Normal}(\mu,\sigma)$
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: 'The only difference from the first equation is that we replace the global $\beta$
    term with product-level betas $\beta_i$. We specify that the unit level elasticity
    $\beta_i$ is drawn from a normal distribution centered around the category-level
    elasticity average $\beta_{c(i)}$, which is drawn from a shared global elasticity
    $\beta_g$ for all groups. For the spread of the distribution $\sigma$, we can
    assume a hierarchical structure for that too, but in this example, we just set
    basic priors for them to maintain simplicity. For this application, we assume
    a prior belief of: ${ \mu= -2, \sigma= 1, \sigma_{c(i)}=1, \sigma_i=1}$. This
    formulation of the prior assumes that the global elasticity is elastic, 95% of
    the elasticities fall between -4 and 0, with a standard deviation of 1 at each
    hierarchical level. To test whether these priors are correctly specified, we would
    do a [prior predictive checks](https://www.pymc.io/projects/docs/en/stable/learn/core_notebooks/posterior_predictive.html)
    (not covered in this article) to see whether our prior beliefs can recover the
    data that we observe.'
  prefs: []
  type: TYPE_NORMAL
- en: This hierarchical structure allows information to flow between products in the
    same category and across categories. If a particular product has limited price
    variation data, its elasticity will be pulled toward the category elasticity $\beta_{c(i)}$.
    Similarly, categories with fewer products will be influenced more by the global
    elasticity, which derives its mean from all category elasticities. The beauty
    of this approach is that the degree of “pooling” happens automatically based on
    the data. Products with lots of price variation will maintain estimates closer
    to their individual data patterns, while those with sparse data will borrow more
    strength from their group.
  prefs: []
  type: TYPE_NORMAL
- en: Implementation
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: In this section, we implement the above model using the Numpyro package in Python,
    a lightweight probabilistic programming language powered by JAX for autograd and
    JIT compilation to GPU/TPU/CPU. We start off by generating our synthetic data,
    defining the model, and fitting the model to the data. We close out with some
    visualizations of the results.
  prefs: []
  type: TYPE_NORMAL
- en: Data Generating Process
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: We simulate sales data where demand follows a log-linear relationship with price
    and the product-level elasticity is generated from a Gaussian distribution $\beta_i
    \sim \textrm{Normal}(-2, 0.7)$. We add in a random price change every time period
    with a $50\%$ probability, category-specific time trends, and random noise. This
    adds in multiplicatively to generate our log expected demand. From the log expected
    demand, we exponentiate to get the actual demand, and draw realized units sold
    from a Poisson distribution. We then filter to keep only units with more than
    100 units sold (helps accuracy of estimates, not a necessary step), and are left
    with $N=11,798$ products over $T = 156$ periods (weekly data for 3 years). From
    this dataset, the true global elasticity is $\beta_g = -1.6$, with category-level
    elasticities ranging from $\beta_{c(i)} \in [-1.68, -1.48]$.
  prefs: []
  type: TYPE_NORMAL
- en: Keep in mind that this DGP ignores a lot of real-world intricacies. We do not
    model any factors that could jointly affect both prices and demand (such as promotions),
    and we do not model any confounders. This example is purely meant to show that
    we can recover the product-specific elasticity under a wells-specified model,
    and does not aim to cover how to correctly identify that price is exogenous. However,
    I suggest that interested readers refer to [Causal Inference for the Brave and
    True](https://matheusfacure.github.io/python-causality-handbook/landing-page.html)
    for an introduction to causal inference.
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE0]'
  prefs: []
  type: TYPE_PRE
- en: 'Filtering summary:'
  prefs: []
  type: TYPE_NORMAL
- en: 'Original number of products: 20000'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: 'Products with > 100 units: 11798'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: 'Products filtered out: 8202 (41.0%)'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: 'Elasticity Information:'
  prefs: []
  type: TYPE_NORMAL
- en: 'Global elasticity: -1.598'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: 'Category elasticities range: -1.681 to -1.482'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '| product | category | time_period | price | units_sold | product_elasticity
    | category_elasticity | global_elasticity | cat_by_time |'
  prefs: []
  type: TYPE_TB
- en: '| --- | --- | --- | --- | --- | --- | --- | --- | --- |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 0 | 125.95 | 550 | -1.185907 | -1.63475 | -1.597683 | 8-0 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 1 | 125.95 | 504 | -1.185907 | -1.63475 | -1.597683 | 8-1 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 2 | 149.59 | 388 | -1.185907 | -1.63475 | -1.597683 | 8-2 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 3 | 149.59 | 349 | -1.185907 | -1.63475 | -1.597683 | 8-3 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 4 | 176.56 | 287 | -1.185907 | -1.63475 | -1.597683 | 8-4 |'
  prefs: []
  type: TYPE_TB
- en: Model
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: We begin by creating indices for products, categories, and category-time combinations
    using `pd.factorize()`. This allows us to seelct the correct parameter for each
    observation. We then convert the price (logged) and units series into JAX arrays,
    then create plates that corresponds to each of our parameter groups. These plates
    store the parameters values for each level of the hierarchy, along with storing
    the parameters representing the fixed effects.
  prefs: []
  type: TYPE_NORMAL
- en: 'The model uses NumPyro’s plate to define the parameter groups:'
  prefs: []
  type: TYPE_NORMAL
- en: '`global_a`: 1 global price elasticity parameter with a $\textrm{Normal}(-2,
    1)$ prior.'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '`category_a`: $C=10$ category-level elasticities with priors centered on the
    global parameter and standard deviation of 1.'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '`product_a`: $N=11,798$ product-specific elasticities with priors centered
    on their respective category parameters and standard deviation of 1.'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '`product_effect`: $N=11,798$ product-specific baseline demand effects with
    a standard deviation of 3.'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '`time_cat_effects`: $(T=156)\cdot(C=10)$ time-varying effects specific to each
    category-time combination with a standard deviation of 3.'
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: We then [reparameterize](https://sassafras13.github.io/ReparamTrick/) the parameters
    using The `LocScaleReparam()` argument to improve sampling efficiency and avoid
    funneling. After creating the parameters, we calculate log expected demand, then
    convert it back to a rate parameter with clipping for numerical stability. Finally,
    we call on the data plate to sample from a Poisson distribution with the calculated
    rate parameter. The optimization algorithm will then find the values of the parameters
    that best fit the data using stochastic gradient descent. Below is a graphical
    representation of the model to show the relationship between the parameters.
  prefs: []
  type: TYPE_NORMAL
- en: '![](../Images/7f0100e5dc6b3db8bed5fde0586a652d.png)'
  prefs: []
  type: TYPE_IMG
- en: '[PRE1]'
  prefs: []
  type: TYPE_PRE
- en: Estimation
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: While there are multiple ways to estimate this equation, we use [Stochastic
    Variational Inference](https://jmlr.org/papers/volume14/hoffman13a/hoffman13a.pdf)
    (SVI) for this particular application. As an overview, SVI is a gradient-based
    optimization method to minimize the KL-divergence between a proposed posterior
    distribution to the true posterior distribution by minimizing the ELBO. This is
    a different estimation technique from [Markov-Chain Monte Carlo](https://www.publichealth.columbia.edu/research/population-health-methods/markov-chain-monte-carlo)
    (MCMC), which samples directly from the true posterior distribution. In real-world
    applications, SVI is more efficient and easily scales to large datasets. For this
    application, we set a random seed, initialize the guide (family of posterior distribution,
    assumed to be a Diagonal Normal), define the learning rate schedule and optimizer
    in Optax, and run the optimization for 1,000,000 (takes ~1 hour) iterations. While
    the model might have converged beforehand, the loss still improves by a minor
    amount even after running the optimization for 1,000,000 iterations. Finally,
    we plot the (log) losses.
  prefs: []
  type: TYPE_NORMAL
- en: '![](../Images/e571c77ccb4afe944b074d5bd0f567f2.png)'
  prefs: []
  type: TYPE_IMG
- en: '[PRE2]'
  prefs: []
  type: TYPE_PRE
- en: '**Recovering Posterior Samples**'
  prefs: []
  type: TYPE_NORMAL
- en: Once the model has been trained, we can can recover the posterior distribution
    of the parameters by feeding in the resulting parameters and the initial dataset.
    We cannot call the parameters `svi_result.params` directly since Numpyro uses
    an affline transformation on the back-end for non-Normal distributions. Therefore,
    we sample 1000 times from the posterior distribution and calculate the mean and
    standard deviation of each parameter in our model. The final part of the following
    code creates a dataframe with the estimated elasticity for each product at each
    hierarchical level, which we then join back to our original dataframe to test
    whether the algorithm recovers the true elasticity.
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE3]'
  prefs: []
  type: TYPE_PRE
- en: '| product | category | time_period | price | units_sold | product_elasticity
    | category_elasticity | global_elasticity | cat_by_time | product_elasticity_svi
    | product_elasticity_svi_std | category_elasticity_svi | category_elasticity_svi_std
    | global_a_svi | global_a_svi_std |'
  prefs: []
  type: TYPE_TB
- en: '| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | ---
    | --- | --- |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 0 | 125.95 | 550 | -1.185907 | -1.63475 | -1.597683 | 8-0 | -1.180956
    | 0.000809 | -1.559872 | 0.027621 | -1.5550271 | 0.2952548 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 1 | 125.95 | 504 | -1.185907 | -1.63475 | -1.597683 | 8-1 | -1.180956
    | 0.000809 | -1.559872 | 0.027621 | -1.5550271 | 0.2952548 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 2 | 149.59 | 388 | -1.185907 | -1.63475 | -1.597683 | 8-2 | -1.180956
    | 0.000809 | -1.559872 | 0.027621 | -1.5550271 | 0.2952548 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 3 | 149.59 | 349 | -1.185907 | -1.63475 | -1.597683 | 8-3 | -1.180956
    | 0.000809 | -1.559872 | 0.027621 | -1.5550271 | 0.2952548 |'
  prefs: []
  type: TYPE_TB
- en: '| 0 | 8 | 4 | 176.56 | 287 | -1.185907 | -1.63475 | -1.597683 | 8-4 | -1.180956
    | 0.000809 | -1.559872 | 0.027621 | -1.5550271 | 0.2952548 |'
  prefs: []
  type: TYPE_TB
- en: Results
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: The following code plots the true and estimated elasticities for each product.
    Each point is ranked by their true elasticity value (black), and the estimated
    elasticity from the model is also shown. We can see that the estimated elasticities
    follows the path of the true elasticities, with a Mean Absolute Error of around
    0.0724\. Points in red represents products whose 95% CI does not contain the true
    elasticity, while points in blue represent products whose 95% CI contains the
    true elasticity. Given that the global mean is -1.598, this represents an average
    error of 4.5% at the product level. We can see that the SVI estimates closely
    follow the pattern of the true elasticities but with some noise, particularly
    as the elasticities become more and more negative. On the top right panel, we
    plot the relationship between the error of the estimated elasticities and the
    true elasticity values. As true elasticities become more and more negative, our
    model becomes less accurate.
  prefs: []
  type: TYPE_NORMAL
- en: For the category-level and global-level elasticities, we can create the posteriors
    using two methods. We can either [boostrap](https://en.wikipedia.org/wiki/Bootstrapping_(statistics) all
    product-level elasticities within the category, or we can get the category-level
    estimates directly from the posterior parameters. When we look at the category-level
    elasticity estimates on the bottom left, we can see that the both the category-level
    estimates recovered from the model and the bootstrapped samples from the product-level
    elasticities are also slightly biased towards zero, with an MAE of ~.033\. However,
    the confidence interval given by the category-level parameter covers the true
    parameter, unlike the bootstrapped product-level estimates. This suggests that
    when determining group-level elasticities, we should directly use the group-level
    parameters instead of bootstrapping the more granular estimates. When looking
    at the global level, both methods contains the true parameter estimate in the
    95% confidence bounds, with the global parameter out-performing the product-level
    bootstrapping, at the cost of having larger standard errors.
  prefs: []
  type: TYPE_NORMAL
- en: '**Considerations**'
  prefs: []
  type: TYPE_NORMAL
- en: '**HB underestimates posterior variance:** One drawback of using SVI for the
    estimation is that it [underestimates the posterior variance](https://arxiv.org/pdf/1903.00617).
    While we will cover this topic in detail in a later article, the objective function
    for SVI only takes into account the difference in expectation of our posited distribution
    and the true distribution. This means that it does not consider the full correlation
    structure between parameters in the posterior. The mean-field approximation commonly
    used in SVI assumes conditional (on the previous hierarchy’s draw) independence
    between parameters, which ignores any covariances between products within the
    same hierarchy. This means that if are any spillover effects (such as cannibalization
    or cross-price elasticity), it would not be accounted for in the confidence bounds.
    Due to this mean-field assumption, the uncertainty estimates tend to be overly
    confident, resulting in confidence intervals that are too narrow and fail to properly
    capture the true parameter values at the expected rate. We can see in the top
    left figure that only 9.7% of the product-level elasticities cover their true
    elasticity. In a later post, we will include some [solutions](https://proceedings.neurips.cc/paper_files/paper/2015/file/4b0a59ddf11c58e7446c9df0da541a84-Paper.pdf)
    to this problem.'
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '**Importance of priors:** When using HB, priors matter significantly more compared
    to standard Bayesian approaches. While large datasets typically allow the likelihood
    to dominate priors when estimating global parameters, hierarchical structures
    changes this dynamic and reduce the effective sample sizes at each level. In our
    model, the global parameter only sees 10 category-level observations (not the
    full dataset), categories only draw from their contained products, and products
    rely solely on their own observations. This reduced effective sample size causes
    shrinkage, where outlier estimates (like very negative elasticities) get pulled
    toward their category means. This highlights the importance of [prior predictive
    checks](https://www.pymc.io/projects/docs/en/stable/learn/core_notebooks/posterior_predictive.html),
    since misspecified priors will have outsized influence on the results.'
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '![](../Images/2bdbc31b6985406518ae6214ad6576ce.png)'
  prefs: []
  type: TYPE_IMG
- en: '[PRE4]'
  prefs: []
  type: TYPE_PRE
- en: Conclusion
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: '**Alternate Uses**: Aside from estimating price elasticity of demand, HB models
    also have a variety of other uses in data science. In retail, HB models can forecast
    demand for existing stores and solve the cold-start problem for new stores by
    borrowing information from stores/networks that have already been established
    and are clustered within the same hierarchy. For recommendation systems, HB can
    estimate user-level preferences from a combination of user and item-level characteristics.
    This structure enables relevant recommendations to new users based on cohort behaviors,
    gradually transitioning to individualized recommendations as user history accumulates.
    If no cohort groupings are easily available, K-means can be used to group similar
    units based on their characteristics.'
  prefs: []
  type: TYPE_NORMAL
- en: Finally, these models can also be used to combine results from experimental
    and observational studies. Scientists can use historical observational uplift
    estimates (ads uplift) and supplement it with newly developed A/B tests to reduce
    the required sample size for experiments by incorporating prior knowledge. This
    approach creates a continuous learning framework where each new experiment builds
    upon previous findings rather than starting from scratch. For teams facing resource
    constraints, this means faster time-to-insight (especially when combined with
    [surrogate models](https://www.nber.org/system/files/working_papers/w26463/w26463.pdf))
    and more efficient experimentation pipelines.
  prefs: []
  type: TYPE_NORMAL
- en: '**Final Remarks**: While this introduction has highlighted several applications
    of hierarchical Bayesian models, we’ve only scratched the surface. We haven’t
    deep dived into granular implementation aspects such as prior and posterior predictive
    checks, formal goodness-of-fit assessments, computational scaling, distributed
    training, performance of estimation strategies (MCMC vs. SVI), and non-nested
    hierarchical structures, each of which deserves their own post.'
  prefs: []
  type: TYPE_NORMAL
- en: Nevertheless, this overview should provide a practical starting point for incorporating
    hierarchical Bayesian into your toolkit. These models offer a framework for handling
    (usually) messy, multi-level data structures that are often seen in real-world
    business problems. As you begin implementing these approaches, I’d love to hear
    about your experiences, challenges, successes, and new use cases for this class
    of model, so please reach out with questions, insights, or examples through [my
    email](/cdn-cgi/l/email-protection#7f0b0d1e111b1a0d1a140b0d163f18101018131a511c1012)
    or [LinkedIn](https://www.linkedin.com/in/derek-tran-ab75ab64/). If you have any
    feedback on this article, or would like to request another topic in causal inference/machine
    learning, please also feel free to reach out. Thank you for reading!
  prefs: []
  type: TYPE_NORMAL
- en: '**N****ote:** All images used in this article is generated by the author.'
  prefs: []
  type: TYPE_NORMAL
