# 测量 C 与 Rust 的执行时间

> 原文：[`towardsdatascience.com/measuring-the-execution-times-of-c-versus-rust-bc45c577052a/`](https://towardsdatascience.com/measuring-the-execution-times-of-c-versus-rust-bc45c577052a/)

### 比较两种最快的编程语言

![由 Aron Visuals 在 Unsplash 上的照片](img/01c4e4fd745322daba2aa2a8d6381f8c.png)

由[Aron Visuals](https://unsplash.com/@aronvisuals?utm_source=medium&utm_medium=referral)在[Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral)上的照片

C 是否比 Rust 快？我总是认为这个问题的答案是肯定的，但最近我感到有必要测试我的假设。我试图搜索有关这个主题的信息，但我无法找到将这两种语言并排比较的信息，所以我进行了自己的测试。我发现了一些情况下，Rust 可以被视为比 C 快。

## **C vs Rust**

C 和 Rust 都是为高性能而设计的（低级）系统编程语言；它们的不同之处在于设计哲学和特性。

+   **C**关于简单性，并给予程序员对内存的完全控制。这种自由可能会带来潜在的问题，如缓冲区溢出、空指针解引用和内存泄漏，如果程序员没有在他们的代码中妥善处理这些问题。它缺乏内置的安全功能来防止这些问题，用户必须手动实现。

+   **Rust**与这正好相反，它强调内存安全，同时尽可能少地牺牲性能。它使用借用检查器和所有权系统，在编译时强制执行规则，以确保内存被安全访问。

虽然 Rust 的主要特性是自动内存安全，但如果程序员需要关闭这一功能，它也允许这样做，这使得它非常灵活。

## **比较速度**

在测试中，我使用了：

+   Debian 10

+   Clang 7.0.1

+   GCC 8.3.0

+   Clang 13.0.1

+   Cargo 1.83.0

+   Rustc 1.83.0

我首先在每个语言中创建了一个 hello world 程序。

**C**

```py
#include <stdio.h>

int main() {
    printf("Hello, World!n");
    return 0;
}
```

**Rust**

```py
fn main() {
    println!("Hello, world!");
}
```

这只是两个程序默认的体验。我没有对任何一个添加任何优化。

```py
gcc hello.c -o helloc
```

```py
cargo build --release
```

当我拥有这两个二进制文件时，我使用了 valgrind massif 工具来获取每个程序的时间和内存使用情况。

**C Hello World 执行**

![valgrind - - tool=massif ./helloc for C](img/ed2bf1d4ba0cd3942495c1f5db985417.png)

valgrind – – tool=massif ./helloc for C

Valgrind 的工作原理是在程序执行过程中进行快照，然后打印出当时使用的总内存量（以字节为单位），以及当前经过的时间。在这种情况下（i）时间是以 CPU 指令来衡量的。我们可以看到，C 代码进行了 4 次总快照，并使用了 131,349 条 CPU 指令来打印 hello world。

我们为什么关心 CPU 指令数？CPU 速度通常以时钟速度来衡量。如今通常是千兆赫兹，这意味着 3.0 GHz 的处理器每秒可以执行 30 亿个时钟周期。然而，一个时钟周期并不等于一条指令。执行一条指令所需的时钟周期数量是一个非常依赖于架构的问题。许多处理器现在甚至可以在一个周期内执行多条指令。但在同一平台上，比如我这里所做的那样，我们可以使用 CPU 执行程序所需的指令数量来了解哪个程序对 CPU 完成工作所需的工作量更少。完成时间和完成工作量有一个大致的关系。

**Rust Hello World 执行**

![valgrind – – tool=massif ./hello for Rust 的输出](img/10d529336a3378dd7120bb2d360daf13.png)

valgrind – – tool=massif ./hello for Rust 的输出

与 C 程序不同，Rust 程序打印 hello world 时总共进行了 14 次快照和 508,165 条总 CPU 指令。从这一点我们可以看出，Rust 程序打印 hello 比 C 程序花费了更多的工作量。

Rust 程序也使用了更多的 RAM，虽然这里没有显示，我还注意到二进制文件的大小存在很大差异，Rust 程序占用 412k，而 C 程序只占用 16k。

这个测试并不意味着 C 比 Rust 快，因为还有方法可以减少 Rust 打印到屏幕所需的时间。我只是先这样做，因为我开始优化和执行操作之前很好奇。

我通过添加以下内容到我的 Cargo.toml 中进一步减少了它：

```py
[profile.release]
opt-level = 3
lto = true
panic = "abort"
codegen-units = 1
target-cpu = "native"
```

使用 std::io::Write 代替 println 以减少一点开销。

```py
use std::io::{self, Write};

fn main() {
    io:stdout().write_all(b"Hello, World!n").unwrap();
}
```

通过这样做，我设法减少了时间，但仍然远远不及 C 程序，但我确信它可以进一步减少。

![valgrind – – tool=massif ./hello for Rust 新优化的 hello 输出 2](img/287816e9f7ec9a9c880fdf05d912b539.png)

valgrind – – tool=massif ./hello for Rust 输出 2

## **快速排序**

在一个程序中非常重要的一点是它执行数学函数的速度有多快。

为了测试这一点，我分别用 Rust 和 C 编写了一个简单的快速排序程序。

为了避免随机数生成对时间的影响，我在两个程序中都使用了相同的 1000 个数字数组。

Rust 代码：

```py
use std::time::Instant;

// QuickSort implementation in Rust
fn quick_sort<T: Ord>(arr: &amp;mut [T]) {
    if arr.len() <= 1 {
        return;
    }
    let pivot_index = partition(arr);
    quick_sort(&amp;mut arr[0..pivot_index]);
    quick_sort(&amp;mut arr[pivot_index + 1..]);
}

fn partition<T: Ord>(arr: &amp;mut [T]) -> usize {
    let pivot_index = arr.len() / 2;
    arr.swap(pivot_index, arr.len() - 1);
    let mut i = 0;
    for j in 0..arr.len() - 1 {
        if arr[j] <= arr[arr.len() - 1] {
            arr.swap(i, j);
            i += 1;
        }
    }
    arr.swap(i, arr.len() - 1);
    i
}

fn main() {
    let numbers: [i32; 1000] = [
        //put array of 1000 numbers here 
    ];
    // Measure time before operation
    let start = Instant::now();

    // Perform quicksort
    let mut numbers = numbers.clone();  // We need a mutable copy for sorting
    quick_sort(&amp;mut numbers);

    // Measure time after operation
    let duration = start.elapsed();
    let seconds = duration.as_secs_f64();  // Convert to seconds as f64
    println!("Sorting 1000 numbers took {:.6} seconds", seconds);

    // Simple operation: sum of numbers
    let start = Instant::now();
    let sum: i32 = numbers.iter().sum();
    let duration = start.elapsed();
    let seconds = duration.as_secs_f64();  // Convert to seconds as f64
    println!("Summing the numbers took {:.6} seconds", seconds);
} 
```

C 代码：

```py
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

// QuickSort implementation in C
void quick_sort(int arr[], int low, int high) {
    if (low < high) {
        int pivot_index = partition(arr, low, high);
        quick_sort(arr, low, pivot_index - 1);  // Sort the left part
        quick_sort(arr, pivot_index + 1, high); // Sort the right part
    }
}

int partition(int arr[], int low, int high) {
    int pivot = arr[low];  // Use the first element as pivot
    int i = low + 1;  // Start the i index just after the pivot
    int j = high;  // Start the j index at the last element

    while (i <= j) {
        // Move the i index to the right while the elements are less than or equal to pivot
        while (i <= high &amp;&amp; arr[i] <= pivot) {
            i++;
        }

        // Move the j index to the left while the elements are greater than the pivot
        while (arr[j] > pivot) {
            j--;
        }

        if (i < j) {
            // Swap arr[i] and arr[j] if they are in the wrong order
            int temp = arr[i];
            arr[i] = arr[j];
            arr[j] = temp;
        }
    }

    // Swap pivot into its correct position
    arr[low] = arr[j];
    arr[j] = pivot;

    return j; // Return the index of the pivot
}

void benchmark() {
    int numbers[1000] = {
   //put array of 1000 numbers here
};

    // Measure time before sorting
    clock_t start = clock();

    // Perform quicksort
    quick_sort(numbers, 0, 999);

    // Measure time after sorting
    clock_t end = clock();
    double sort_duration = (double)(end - start) / CLOCKS_PER_SEC;
    printf("Sorting 1000 numbers took %f secondsn", sort_duration);

    // Sum of array elements
    start = clock();
    long sum = 0;
    for (int i = 0; i < 1000; i++) {
        sum += numbers[i];
    }
    end = clock();
    double sum_duration = (double)(end - start) / CLOCKS_PER_SEC;
    printf("Summing the numbers took %f secondsn", sum_duration);
}

int main() {
    benchmark();
    return 0;
} 
```

我随后用以下参数构建了这两个程序：

```py
Cargo build --release
```

```py
gcc quicksort.c -o quicksort
```

根据第一次测试，我预期 C 代码会更快，但令我惊讶的是，Rust 代码显示它运行得更快。

![C quicksort 输出](img/27bb37ffde04cd0a2dc2880d1686bcd7.png)

C 快速排序输出

![Rust Quicksort 输出](img/2cea7e2bba9698600b99b94b8b7ae723.png)

Rust Quicksort 输出

令我困惑的是，当我运行了这两个二进制文件并通过 valgrind 进行检查时，它报告说 C 二进制文件仍然使用了更少的指令。

![C valgrind 输出，显示总执行时间为 434,078](img/62624dc6e848a6c6d498c25146edaeb0.png)

C valgrind 输出的总执行时间为 434,078

![Rust Valgrind 输出的总执行时间为 656,647](img/02ffa40633aea4836b53c79edb8e41c4.png)

Rust Valgrind 输出的总执行时间为 656,647

由于 valgrind 只测量执行过程中使用的总指令数，这让我怀疑这与 Rust 优化二进制文件的方式有关。这促使我尝试使用不同的编译器和优化标志来编译 C，看看是否是这个问题。

```py
gcc -O3 -funroll-loops -march=native -flto -fomit-frame-pointer -DNDEBUG -o sort sort.c
```

![使用优化后的 gcc 尝试](img/10714c9d99168aca1b50e3dced9483bd.png)

使用优化后的 gcc 尝试

```py
clang -Ofast -march=native -flto -funroll-loops -ftree-vectorize -fno-exceptions -fomit-frame-pointer -fno-math-errno -fstack-protector-strong -o sort sort.c
```

![使用优化后的 clang 尝试](img/dfa04e908a53cc668087acbea4150a4c.png)

使用优化后的 clang 尝试

```py
clang-13 -Ofast -march=native -flto -funroll-loops -ftree-vectorize -fno-exceptions -fomit-frame-pointer -fno-math-errno -fstack-protector-strong -o sort sort.c
```

![使用优化后的 clang 13 尝试](img/1ab7a7be0f2dd1d592da887bbf18f16d.png)

使用优化后的 clang 13 尝试

我尝试过的每一件事都表明 Rust 二进制文件比 C 二进制文件性能更好。

这让我怀疑 Rust 定时器是否比 C 定时器快，所以我写了一个简单的程序来测试在一秒内尽可能迭代一个数字。

**C 代码：**

```py
#include <stdio.h>
#include <time.h>

int main() {
    clock_t start = clock();
    clock_t end = start;
    int count = 0;

    // Loop until 1 second has passed
    while (((double)(end - start)) / CLOCKS_PER_SEC < 1.0) {
        count++;
        end = clock();
    }

    printf("The number after 1 second is: %dn", count);
    return 0;
}
```

**Rust 代码：**

```py
use std::time::{Instant, Duration};

fn main() {
    let start = Instant::now();
    let mut count = 0;

    // Loop until 1 second has passed
    while start.elapsed() < Duration::new(1, 0) {
        count += 1;
    }

    println!("The number after 1 second is: {}", count);
}
```

**编译标志：**

```py
cargo build --release
```

```py
clang-13 -Ofast -march=native -flto -funroll-loops -ftree-vectorize -fno-exceptions -fomit-frame-pointer -fno-math-errno -fstack-protector-strong -o timer timer.c
```

我发现 Rust 程序的数字总是比 C 的要大。这表明 Rust 完成循环的速度比 C 程序快。

![C 定时器完成循环](img/97fef8a831e1af7832ffe25961cb84dd.png)

C 定时器完成循环

![Rust 定时器完成循环](img/ee43d882409f5713278d07dce030d1e6.png)

Rust 定时器完成循环

无论我尝试什么，这似乎总是如此。此时，要弄清楚为什么会出现这种情况的唯一方法就是查看实际的汇编代码。

我在 Ghidra 中反汇编了这两个程序，并找到了迭代一秒数字的函数。

Rust 汇编中的循环：

![Rust 定时器反汇编](img/b25b806c1dc448d44fd0d0f4b7c585e8.png)

Rust 定时器反汇编

C 汇编中的循环：

![C 定时器反汇编](img/7cf01f7f9c8cf215adf2f64786c91054.png)

C 定时器反汇编

反汇编输出中的每一行都对应 CPU 需要执行的一条指令。

我发现的是 Rust 和 C 处理时间的方式不同。Rust 的优化要好得多。

C 代码在一个循环中必须执行更多的指令来计算当前时间是否仍然小于一秒，而 Rust 代码则不需要。

查看 Rust 汇编让我意识到我的 C 代码应该进一步优化，但不是由编译器，而是由我来优化。

这里是新的 C 计数器。

```py
#include <stdio.h>
#include <time.h>

int main() {
    time_t start = time(NULL);  // Get the current time in seconds
    time_t end = start;
    int count = 0;

    // Loop until 1 second has passed
    while (end - start < 1) {
        count++;
        end = time(NULL);  // Only check the time once per iteration
    }

    printf("The number after 1 second is: %dn", count);
    return 0;
}
```

编译标志与上面相同。

![](img/7ad72a0b67840fdb042b61ec4bfd189c.png)

新的计数器迭代速度比前两个都要快得多。

当我查看反汇编版本时，我发现它与 Rust 的看起来很相似。

![新的 C 定时器反汇编](img/662d57ad194f324622690433e020035f.png)

新的 C 定时器反汇编

唯一的问题是这段代码不是在 1 秒内运行的。它实际上运行得比一秒还快。

我使用 bash 脚本来测试执行前后的系统时间来测试这一点。

```py
#!/bin/bash

# Capture start time
start=$(date +%s%N)  # Get time in nanoseconds

# Run the program (replace 'your_program' with the actual program)
./newtimer

# Capture end time
end=$(date +%s%N)

# Calculate the time difference in nanoseconds
duration=$((end - start))

# Convert duration to milliseconds (optional, if you need a more readable format)
ms=$((duration / 1000000))

echo "Program took $ms milliseconds to complete." 
```

运行后我发现，新的计时器完成时间远远小于一秒：

![新计时器执行速度](img/a407dec2a589b55c9992d89977509002.png)

新计时器执行速度

更糟糕的是，当代码被缓存时，它的执行速度会远远超过 1 秒，这使得 clock()，尽管在循环中做了更多的工作，但在尝试在 C 中停止一段时间时，是更好的选择。

两个原始的 C 和 Rust 代码完成的时间大致相同，大约为一秒：

**C 计数器执行时间：**

![C 计时器 execute.sh](img/05b37c2ab1ed56ccd665e84ac6569f7b.png)

C 计时器 execute.sh

**Rust 计数器执行时间：**

![Rust 计时器 execute.sh](img/8ecfd717c670ccf2939e0cfc8758584c.png)

Rust 计时器 execute.sh

因此，虽然使用时间函数对 C 代码进行反汇编看起来与 Rust 的类似，但 Rust 的时间函数在功能上与 C 的时钟不同。

这三个时钟获取时间的方式略有不同，性能差异与此有关。我应该在某个时候深入研究这三个时钟。

我认为这表明 Rust 的时钟更优化。

尽管如此，这些都没有回答我的原始问题，即 Rust 是否实际上比 C 更快地执行快速排序算法。

如果是时钟计算减慢了速度，那么我只需要增加数组中的项目数量，我们就会看到它们之间的差异增大，因为时钟只为快速排序计算减慢了固定的时间，所以如果我使用更多的项目，我们就会看到 C 代码执行得更快。

旁注：我发现 C 的时钟可以在不转换为双精度的情况下执行，但 Rust 代码仍然更快。

```py
#include <stdio.h>
#include <time.h>

int main() {
    clock_t start = clock();
    clock_t end = start;
    int count = 0;

    // Get the number of clock ticks per second
    clock_t ticks_per_second = CLOCKS_PER_SEC;

    // Loop until 1 second of CPU time has passed
    while ((end - start) < ticks_per_second) {
        count++;
        end = clock();  // Update the clock value once per iteration
    }

    printf("The number after 1 second is: %dn", count);
    return 0;
}
```

## 使用 Rust 和 C 作为快速排序的外部库

为了解决这个问题，我决定将 Rust 和 C 都做成外部库，我从 Python 脚本中调用。这样，我就可以使用 Python 的计时器来确定哪个执行得更快，并且我可以让 Python 生成大量随机生成的数字数组，供两者使用。

Python 代码：

```py
import ctypes
import random
import time

# Load the C library
c_lib = ctypes.CDLL('./libquicksort.so')

# C function prototypes
c_lib.quicksort.argtypes = [ctypes.POINTER(ctypes.c_int), ctypes.c_int, ctypes.c_int]

# Load the Rust library
rust_lib = ctypes.CDLL('./quicksort/target/release/libquicksort.so')

# Rust function prototype
rust_lib.quicksort.argtypes = [ctypes.POINTER(ctypes.c_int), ctypes.c_int]

def test_quicksort():
    # Test with a random list of 100,000,000 integers
    size = 100000000
    arr = [random.randint(0, 10000) for _ in range(size)]

    # C QuickSort
    c_arr = (ctypes.c_int * size)(*arr)
    start_time = time.time()
    c_lib.quicksort(c_arr, 0, size - 1)
    c_time = time.time() - start_time
    print(f"C QuickSort completed in {c_time:.6f} seconds")

    # Rust QuickSort
    rust_arr = (ctypes.c_int * size)(*arr)
    start_time = time.time()
    rust_lib.quicksort(rust_arr, size)
    rust_time = time.time() - start_time
    print(f"Rust QuickSort completed in {rust_time:.6f} seconds")

    # C QuickSort
    c_arr = (ctypes.c_int * size)(*arr)
    start_time = time.time()
    c_lib.quicksort(c_arr, 0, size - 1)
    c_time = time.time() - start_time
    print(f"C QuickSort completed in {c_time:.6f} seconds")

    # Rust QuickSort
    rust_arr = (ctypes.c_int * size)(*arr)
    start_time = time.time()
    rust_lib.quicksort(rust_arr, size)
    rust_time = time.time() - start_time
    print(f"Rust QuickSort completed in {rust_time:.6f} seconds")

    # Check if both results are the same
    start_time = time.time()
    pythonsorttime = sorted(arr)
    python_time = time.time() - start_time
    print(f"Python sort completed in {python_time:.6f} seconds")

    assert list(c_arr) == sorted(arr), "C QuickSort failed!"
    assert list(rust_arr) == sorted(arr), "Rust QuickSort failed!"
    print("Both QuickSort implementations worked correctly!")

if __name__ == '__main__':
    test_quicksort() 
```

Rust 库

```py
 #[no_mangle]
pub extern "C" fn quicksort(arr: *mut i32, len: usize) {
    let slice = unsafe { std::slice::from_raw_parts_mut(arr, len) };
    quicksort_recursive(slice);
}

fn quicksort_recursive<T: Ord>(arr: &amp;mut [T]) {
    if arr.len() <= 1 {
        return;
    }
    let pivot_index = partition(arr);
    quicksort_recursive(&amp;mut arr[0..pivot_index]);
    quicksort_recursive(&amp;mut arr[pivot_index + 1..]);
}

fn partition<T: Ord>(arr: &amp;mut [T]) -> usize {
    let pivot_index = arr.len() / 2;
    arr.swap(pivot_index, arr.len() - 1);
    let mut i = 0;
    for j in 0..arr.len() - 1 {
        if arr[j] <= arr[arr.len() - 1] {
            arr.swap(i, j);
            i += 1;
        }
    }
    arr.swap(i, arr.len() - 1);
    i
} 
```

Cargo.toml

```py
[package]
name = "quicksort"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = 3
lto = true
panic = 'abort'
codegen-units = 1
target-cpu = "native"

[dependencies]

[lib]
crate-type = ["cdylib"]  # This is the key part, specifying the shared library
```

C 库

```py
// quicksort.c
void quicksort(int arr[], int low, int high) {
    if (low < high) {
        int pivot_index = partition(arr, low, high);
        quicksort(arr, low, pivot_index - 1);
        quicksort(arr, pivot_index + 1, high);
    }
}

int partition(int arr[], int low, int high) {
    int pivot = arr[high];
    int i = low - 1;

    for (int j = low; j < high; j++) {
        if (arr[j] <= pivot) {
            i++;
            int temp = arr[i];
            arr[i] = arr[j];
            arr[j] = temp;
        }
    }
    int temp = arr[i + 1];
    arr[i + 1] = arr[high];
    arr[high] = temp;
    return i + 1;
} 
```

编译标志：

```py
cargo build --release
```

```py
gcc -shared -o libquicksort.so -fPIC -O3 -march=native -flto -funroll-loops quicksort.c
```

我随后构建并运行了程序：

![C 与 Rust 库 Python 速度对比](img/12fc37a7c06f9f5f323896ba22f5cd45.png)

C 与 Rust 库 Python 速度

我发现的是，C 实现的快速排序比 Rust 实现的更快。我在数组中使用了十亿个数字，这样差异会更加明显。此外，您可能会注意到 Python 排序函数的完成速度比 Rust 或 C 库都要快得多；这是因为 Python 使用了 Timsort，其最佳情况为 O(n)。而快速排序的最佳情况只是 O(n log n)。

![快速排序和 Timsort 运行时间比](img/56553283eaffead054f9498626f45fc2.png)

快速排序和 Timsort 运行时间比

当我们解决这个问题时，大致显示了两者之间的关系。

## 总结

从所有这些中我学到的是 C 和 Rust 之间的差距并不大。C 通常比 Rust 快，使用的内存更少，占用的空间也更小，但用户在 C 中很容易犯一个错误，这可能会逆转这一点。C 代码可能比 Rust 代码慢。这实际上使得在两者之间做出选择更多地基于其他因素，而不仅仅是速度，因为它们非常相似。

真正重要的是为任务选择的算法。

感谢阅读，并告诉我你的想法！

Aaron

## **参考文献**

使用的代码可以在我的 GitHub [这里](https://github.com/ambeckley/ComparingCandRustQuicksort/tree/main)找到
