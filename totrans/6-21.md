# 线性规划：辅助变量

> 原文：[`towardsdatascience.com/linear-programming-auxiliary-variables-c66bb66c6aee/`](https://towardsdatascience.com/linear-programming-auxiliary-variables-c66bb66c6aee/)

![图片来源：pexels.com by Pixababy](img/b81956ee75c75fa498146745f1fad517.png)

图片来源：pexels.com by Pixababy

辅助变量似乎是在许多线性规划材料中经常被快速跳过的主题。我认为它们非常有趣且相当强大。正因为如此，我决定写一篇短文来解释和演示辅助变量在线性规划中通常是如何被使用的。

在我们深入探讨辅助变量之前——我想提到这是我在线性规划（LP）上撰写的一系列文章的第五部分。要查看我已覆盖的其他 LP 主题，请使用下面的链接：

> [**线性规划**](https://medium.com/@jarom.hulet/list/fe5c1fba2583)

首先，让我们来回答一个问题——什么是**辅助变量**？我在线性规划（LP）的背景下对辅助变量的定义是：“添加到线性规划问题中，允许使用其他情况下不可能的逻辑的额外变量。”

> 辅助变量：添加到线性规划问题中，允许使用其他情况下不可能的逻辑的额外变量。

通常，辅助变量以巧妙的方式用于构建目标函数和约束中的非线性关系（通常是违反线性规划规则），同时仍然适用于线性规划。换句话说，它们允许线性化那些没有它们就会是非线性的函数。由于使用辅助变量的方式有多种，在这篇文章中，我们将探讨我见过的最常见的方法。我的目标是让你对辅助变量做什么以及如何在线性规划中使用它们有一个良好的直觉。文章的其余部分将通过详细示例来介绍辅助变量的具体应用。请注意，这并不是对辅助变量在线性规划中使用的详尽描述。它的目的是展示它们最常用的几种方式。

### 创建分段线性函数

在分段线性方程中发生的跳跃在线性规划中是不被允许的。幸运的是，我们可以使用辅助变量来实现分段函数的等效功能！让我们来看一个例子。

让我们想象一下，你管理一个制造工厂。你需要生产一定数量的产品，同时最小化你公司的工资支出。每个员工都有一个时薪和一个每小时的生产率。你的目标是创建一个工作计划，以最小化工资支出，同时满足特定的单位生产数量。这很容易吗？有一个问题！任何每周工作超过 20 小时的员工都有资格获得福利——这些福利将额外花费每个员工 500 美元。这代表了函数斜率（时薪）相同的跳跃。

我们将首先探讨如何执行截距跳跃。让我们设置一个没有截距跳跃的简单问题，然后我们可以添加在 20 小时工作后的额外福利成本复杂性。在我们的简单例子中，我们有三个员工，他们每人有不同的工资和不同的生产力水平（如下表所示）。我们的目标是同时生产至少 600 个单位，同时最小化劳动力成本。

![图片由作者提供](img/792a84069c6e908691080ced64da6a01.png)

图片由作者提供

在考虑额外福利成本复杂性之前，我们的目标函数和约束条件如下：

![截距跳跃前的线性规划设置 - 1 个目标函数，1 个约束](img/472cd32d8eb034e52eb227d0f5fbee75.png)

截距跳跃前的线性规划设置 - 1 个目标函数，1 个约束

好的，在 LP 101 问题设置完成后，让我们来添加 20 小时的截距跳跃。我们将使用所谓的“大 M 方法”来完成这个任务。在这个方法中，我们创建一个或多个二元辅助变量，并创建一个额外的约束来将辅助变量绑定到原始决策变量。在约束配置中，“大 M”中的“M”部分发挥作用。

下面是我们将添加的新约束的例子 - *y* 是我们添加到问题中的二元辅助变量，M 是一个任意大的数。

![大 M 约束](img/4dae81fc0b210c6621eff21dea5e022c.png)

大 M 约束

让我们分解这个约束，因为这是执行跳跃的主要概念。在左边，我们有安排员工 1 工作小时数的决策变量。在右边，我们有需要福利的小时数加上‘M’，这是一个任意大的数（因此称为 _big-_M），而 y1 是我们的二元辅助变量。在我们的例子中，我将 M 设为 1,000。理解这个约束是关键！让我们通过运行不同值来通过员工 1 小时的不等式来建立我们的直觉。请看下表和下面的解释：

![大 M 方法的解释 - 图片由作者提供](img/4ed9090a29c0050eb10ee5d1bfd24e63.png)

大 M 方法的解释 - 图片由作者提供

除了新的约束条件外，我们还必须对我们的目标函数进行修改。这很简单做到，我们将我们的新辅助变量（记住，它们是二进制的）和截距跳跃的乘积（在这种情况下是 500 美元）添加到目标函数中。当辅助变量为 0 时，不会增加额外成本。当员工工作超过 20 小时时，辅助变量被迫为 1，并将 500 美元添加到该员工的客观价值中。

![更新后的截距跳跃目标函数](img/0cc97d17f88e647cc6bfe7c9cf3bfbda.png)

更新后的截距跳跃目标函数

问题完全表述后，让我们使用 pulp 包在 Python 中设置并解决它！

```py
import pulp

# Define the problem
problem = pulp.LpProblem("Staff_Management", pulp.LpMinimize)

# Define the decision variables as integers
empl1 = pulp.LpVariable("empl1", lowBound=0, upBound=60)
empl2 = pulp.LpVariable("empl2", lowBound=0, upBound=60)
empl3 = pulp.LpVariable("empl3", lowBound=0, upBound=60)

# establish employee pay and productivity
empl1_pay = 15
empl2_pay = 18
empl3_pay = 22

empl1_prod = 7
empl2_prod = 8
empl3_prod = 10

# create auxiliary variables to capture piecewise OT function
empl1 = pulp.LpVariable("empl1_reg", lowBound=0, upBound=40)
empl2 = pulp.LpVariable("empl2_reg", lowBound=0, upBound=40)
empl3 = pulp.LpVariable("empl3_reg", lowBound=0, upBound=40)

# add auxiliary variables
y1 = pulp.LpVariable("y1", lowBound=0, cat="Integer")
y2 = pulp.LpVariable("y2", lowBound=0, cat="Integer")
y3 = pulp.LpVariable("y3", lowBound=0, cat="Integer")

# Objective function
problem += empl1_pay*empl1 + 500*y1 +  empl2_pay*empl2 + 500*y2 + empl3_pay*empl3 + 500*y3  , "Salary Cost"

# Constraints
# force ret and ot hours to add up to total hours
M = 1000

# big M method
problem += empl1 <= 20 + M*y1
problem += empl2 <= 20 + M*y2
problem += empl3 <= 20 + M*y3

problem += (empl1_prod * empl1 + 
            empl2_prod * empl2 + 
            empl3_prod * empl3 >= 600)

# Solve the problem
status = problem.solve()

# Output the results
print(f"Status: {pulp.LpStatus[status]}")
print(f"Optimal value of empl1: {empl1.varValue}")
print(f"Optimal value of empl2: {empl2.varValue}")
print(f"Optimal value of empl3: {empl3.varValue}")
print(f"Minimized salary cost: {pulp.value(problem.objective)}")
```

这里是运行结果，看起来我们可以在产生 600 个小部件的同时，产生 1,810 美元的劳动力成本。员工 1 将是唯一获得福利的员工（员工 2 的工时略低于 20 小时以上的阈值）。

![优化输出 - 作者图片](img/44f85353286a35efcb2afc066b8cd8ed.png)

优化输出 – 作者图片

好的，现在我们已经解决了额外福利费用的问题，让我们通过添加加班费（常规工资的 1.5 倍）使事情变得更加复杂！这实际上会相当简单 😁。为此，我们需要将员工小时变量分成两个单独的变量（常规小时和加班小时）。所以现在，我们有了六个员工小时变量，而不是三个。每个员工都有一个常规小时变量和一个加班小时变量。我们创建一个约束条件，将常规小时限制在不超过 40 小时（在 pulp 中，您可以使用内置的 upBound 参数做到这一点）。然后我们将修改目标函数以反映这种变化——我将在下面的代码中复制/粘贴一个摘录：

![具有常规时间和加班时间分割成两个辅助变量的新目标函数](img/518b56a6867d6984d79b6f6ed091e56a.png)

具有常规时间和加班时间分割成两个辅助变量的新目标函数

注意，由于加班费比常规时间贵，求解器在开始添加加班费之前总是会最大化常规时间，因此我们不需要一个约束条件来强制这种行为。我还提高了生产要求至 1,000，以便我们的例子需要一些加班。

这是解决包含福利和加班部分的完整问题的代码：

```py
import pulp

# Define the problem
problem = pulp.LpProblem("Staff_Management", pulp.LpMinimize)

# establish employee pay and productivity
empl1_pay = 15
empl2_pay = 18
empl3_pay = 22

empl1_prod = 7
empl2_prod = 8
empl3_prod = 10

# create auxiliary variables to capture piecewise OT function
empl1_reg = pulp.LpVariable("empl1_reg", lowBound=0, upBound=40)
empl2_reg = pulp.LpVariable("empl2_reg", lowBound=0, upBound=40)
empl3_reg = pulp.LpVariable("empl3_reg", lowBound=0, upBound=40)

empl1_ot = pulp.LpVariable("empl1_ot", lowBound=0, upBound=20)
empl2_ot = pulp.LpVariable("empl2_ot", lowBound=0, upBound=20)
empl3_ot = pulp.LpVariable("empl3_ot", lowBound=0, upBound=20)

# add auxiliary variables
y1 = pulp.LpVariable("y1", lowBound=0, cat="Integer")
y2 = pulp.LpVariable("y2", lowBound=0, cat="Integer")
y3 = pulp.LpVariable("y3", lowBound=0, cat="Integer")

# Objective function
problem += (empl1_pay*empl1_reg + 500*y1 + empl1_ot*1.5*empl1_pay +
            empl2_pay*empl2_reg + 500*y2 + empl2_ot*1.5*empl2_pay + 
            empl3_pay*empl3_reg + 500*y3 + empl3_ot*1.5*empl3_pay 
            , "Salary Cost")

# Constraints
# force ret and ot hours to add up to total hours
M = 1000

# big M method
problem += (empl1_reg + empl1_ot) <= 20 + M*y1
problem += (empl2_reg + empl2_ot) <= 20 + M*y2
problem += (empl3_reg + empl3_ot) <= 20 + M*y3

# constraint on minimum items produced
problem += empl1_prod * (empl1_reg + empl1_ot) + empl2_prod * (empl2_reg + empl2_ot) + empl3_prod * (empl3_reg + empl3_ot) >= 1000

# Solve the problem
status = problem.solve()

# Output the results
print(f"Status: {pulp.LpStatus[status]}")
print(f"Optimal value of empl1 reg: {empl1_reg.varValue}")
print(f"Optimal value of empl1 ot: {empl1_ot.varValue}")
print(f"Optimal value of empl2 reg: {empl2_reg.varValue}")
print(f"Optimal value of empl2 ot: {empl2_ot.varValue}")
print(f"Optimal value of empl3 reg: {empl3_reg.varValue}")
print(f"Optimal value of empl3 ot: {empl3_ot.varValue}")
print(f"Minimized salary cost: {pulp.value(problem.objective)}")
```

这里是最佳输出：

![优化输出 - 作者图片](img/902df6c8b5c528cb4690d262edf6c82e.png)

优化输出 – 作者图片

最佳策略是将员工 1 的工作时间最大化至 60 小时，使用员工 2 达到福利上限，并让员工 3 全周工作再加 2 小时。我希望这个例子已经说明了我们如何将截距跳跃和斜率变化线性化，以便将它们纳入线性规划问题中。

### 变量之间的条件关系

有时候我们需要对变量之间的条件关系进行建模以解决优化问题。在这里，我将深入探讨我为之前写的一篇关于模拟的文章所创建的一个例子（下文有链接）。那篇文章是关于模拟数据的，并间接提到了线性规划（LP）。我完全没有提到它所使用的辅助变量——我想借此机会在这里补充我之前没有提到的内容。

> [**模拟数据，真实学习：情景分析**](https://towardsdatascience.com/simulated-data-real-learnings-scenario-analysis-02ee56ed8886)

下面是如何设置这个问题的——想象一下你是一位正在规划住宅分区的开发商。你有多套不同的平面图可供选择，并且你希望在特定的约束条件下优化你的利润。下面是一个表格，列出了不同的平面图及其关键指标：

![每个平面图的详细信息 - 作者图片](img/aef695f024894c3eb3291705ef10e7b9.png)

每个平面图的详细信息——作者图片

我们的主要决策变量是上表所示每一行的整数值。这些整数值代表我们将建造的每种平面图的房子数量。这在本节末尾的代码中的列表“x”中表示。

下面是该问题的三个约束：

+   我们必须在我们的社区中至少有 6 种不同的平面图。

+   我们至少需要 10 套小于 2,000 平方英尺的住宅，15 套在 2,000 平方英尺到 3,000 平方英尺之间，以及 10 套大于 3,000 平方英尺的住宅。

+   最后，我们有 150,000 平方英尺的用地——每栋房子需要比平面图多 25%的平方英尺——我们不能建造比我们有土地更多的房子！

第一个约束将需要辅助变量，其他两个可以直接通过输入数据和主要决策变量（“x”）来处理。

在我们设置这个约束之前，让我们先了解主要决策变量是如何工作的。上表中的每一行都将有一个自己的“x”决策变量。因此，我们将有 15 个“x”变量——每个将代表将建造的对应平面图的房子数量。例如，X1 代表将建造平面图 1 房子的数量。所以，如果 X1 = 10，那么在住宅分区中将建造 10 栋平面图 1 的房子。

好的，现在让我们进入对话的核心——辅助变量！我们需要确保至少有 6 个“x”变量大于 0。我们通过为每个平面图创建一个二进制辅助变量来实现这一点。因此，我们现在有 15 个二进制“y”变量。接下来，我们需要将“y”变量与“x”变量联系起来，以便当“x”变量大于 0 时，相应的“y”变量被设置为 1。我们通过创建一个约束 x ≥ y 来实现这一点——让我们通过下表来建立对这个约束满足我们需求的理解：

![解释将辅助变量（y）与决策变量（x）关联的约束 - 作者图片](img/56c962e0219090ea43f60ab0a97db0c3.png)

解释将辅助变量（y）与决策变量（x）连接的约束 - 图片由作者提供

在设置好辅助变量并引入新的约束以将 x 与 y 连接起来之后，我们需要添加一个最终的约束——即 y 变量需要至少相加到 6。有了这个约束，我们现在可以设置并运行我们的优化了！

下面的代码展示了如何在 Python 中设置和解决此问题：

```py
import pandas as pd
import numpy as np
from pulp import *

df = pd.read_csv(csv_path)
n = len(df)

# create dummy indicators to categorize home size
df['small_house'] = np.where(df['square_feet'] < 2000, 1, 0)
df['medium_house'] = np.where((df['square_feet'] >= 2000) &amp; 
                              (df['square_feet'] < 3000), 
                               1, 0)
df['large_house'] = np.where(df['square_feet'] >= 3000, 1, 0)

# Create a MILP problem
problem = LpProblem("Simple MILP Problem", LpMaximize)

# Define decision variables
x = LpVariable.dicts("x", range(n), lowBound=0, cat='Integer')
y = LpVariable.dicts('y', range(n), cat='Binary')

# Objective 
problem += lpSum(x[i]*df['gain_loss'][i] for i in range(n))

# constraints

# limit to amount of land available
# assume each floorplan takes 25% more land than its square footage
problem += lpSum(x[i]*df['square_feet'][i]*1.25 for i in range(n)) <= 150000

# requirements for diversity in home sizes
problem += lpSum(x[i]*df['small_house'][i] for i in range(n)) >= 15
problem += lpSum(x[i]*df['medium_house'][i] for i in range(n)) >= 15
problem += lpSum(x[i]*df['large_house'][i] for i in range(n)) >= 10

# Create at least 6 unique floorplans
for i in range(n):
    # if x is 0, y has to be 0
    problem += x[i] >= y[i]

# if x = 1, y coud be 0 or 1
# but because we want sum(y) to be >= 6, the optimization
# will assign y to be 1
problem += lpSum(y[i] for i in range(n)) >= 6

# solve problem
problem.solve()

# print solution
for i in range(n):
    print(f'{i + 1} : {value(x[i])}')

# print optimal profit
print("Optimal profit :", value(problem.objective))
```

这里是最佳解决方案：

![最佳布局策略 - 图片由作者提供](img/d4e26139167e52735a061ac644dc5ec5.png)

最佳布局策略 - 图片由作者提供

我们可以看到，我们的约束是有效的！有 6 个布局被选中以在最佳输出中构建。

### “或”逻辑

在线性规划中，我们经常需要访问“或”逻辑。原始的“或”逻辑不是线性的，因此我们必须使用辅助变量来线性化它。为此，我们需要为“或”逻辑中的每个条件添加一个辅助变量，然后添加另一个辅助变量来将它们连接起来。

想象你管理着一个微芯片制造厂。出于国家安全原因，政府为特定水平的芯片生产提供 2000 美元的补助金。要获得补助金资格，你每周至少需要生产 42 个芯片 A 或 15 个芯片 B。你每周只能获得一次补助金，所以生产超过 42 个 A 和超过 15 个 B 不会得到双倍补助金。这是一个“或”逻辑的例子，因为如果你满足 A 或 B 的要求，你就能获得补助金。

为了制定这个问题，我们需要为每种芯片类型设置一个**二元**辅助变量。我们将创建一个“grant_a”变量和一个“grant_b”变量。然后，我们将添加约束 chip_a ≥ 45 * grant_a——其中 chip_a 是生产的芯片 a 的总数。我们将为芯片 b 添加相同的约束，对应于获得补助金所需的芯片数量。

![带有“grant a”和“grant b”辅助变量的约束 - 图片由作者提供](img/0023eca1774ce0528307b80e5c326761.png)

带有“grant a”和“grant b”辅助变量的约束 - 图片由作者提供

最后，我们需要一种方法来使用“或”逻辑将 grant_a 和 grant_b 连接起来。为此，我们将创建一个额外的**二元**辅助变量——“grant”——以及一个额外的约束。

![将 grant a 和 b 辅助变量连接到 grant 辅助变量的约束 - 图片由作者提供](img/10744fe9aa4066375b23790947359c5b.png)

将 grant a 和 b 辅助变量连接到 grant 辅助变量的约束 - 图片由作者提供

这将迫使 grant 在 grant_a 和 grant_b 都为 0 时为 0。但是，如果 grant_a 和/或 grant_b 为 1，grant 也可以取 1 的值。优化将在有选择的情况下始终将 grant 设置为 1（再次强调，这是当 grant_a 和/或 grant_b 为 1 时），因为将 grant 设置为 1 将增加目标函数 2000 美元！

下面是 Python 代码示例。请注意，我已将边际利润添加到目标函数中（芯片 A 为$20，芯片 B 为$30）并添加了一个材料使用约束来绑定问题。

```py
from pulp import LpProblem, LpMaximize, LpVariable, LpBinary, lpSum

# Define the problem
problem = LpProblem("chip_manufacturing", LpMaximize)

# Decision variables
chip_a = LpVariable("chip_a", lowBound=0, cat="integer")
chip_b = LpVariable("chip_b", lowBound=0, cat="integer")

# set up three auxiliary variables
# one for if the factory qualfies for a grant through chip a production
# one for if the factory qualifies for a grant through chip b production
# and one more to indicate of at least one of the chip production levels qualifies for the grant
grant = LpVariable("grant", cat="Binary")
grant_a = LpVariable("grant_a", cat="Binary")
grant_b = LpVariable("grant_b", cat="Binary")

# Objective function
profit = 20 * chip_a + 30 * chip_b + 2000 * grant
problem += profit, "total profit"

# Constraints
# Material usage and availability
problem += 5 * chip_a + 12 * chip_b <= 200, "raw_material_constraint"

# Grant eligibility conditions
# If 100 units of chip_a are made, grant can be awarded
problem += chip_a >= 45 * grant_a, "grant_chip_A_constraint"

# If 75 units of chip_b are made, grant can be awarded
problem += chip_b >= 15 * grant_b, "grant_chip_b_constraint"

# if grant_a and grant_b are 0, force grant to be 0
problem += grant_a + grant_b >= grant, "grant_or_condition"

# Solve the problem
problem.solve()

# Output the results
print(f"Status: {problem.status}")
print(f"Profit: ${problem.objective.value():,.2f}")
print(f"Chip A: {chip_a.value():.0f} units")
print(f"Chip B: {chip_b.value():.0f} units")
print(f"Grant Awarded: {'Yes' if grant.value() else 'No'}")
```

优化后的解决方案如下：

![优化结果 - 作者提供的图片](img/a7233c70dcf8aced26b33cd9a68b8044.png)

优化结果 – 作者提供的图片

在这里，我们可以看到，尽管我们使用芯片 A 每单位原材料能制造出更多的产品，但我们仍需要制造 15 个单位的芯片 B 以获得资助。一旦我们获得资助，我们就用剩下的材料来生产芯片 A。看起来“或”逻辑是正确的！我们现在明白了如何使用辅助变量来解决带有“或”逻辑的线性规划问题！

### 结论

我希望你们对如何使用辅助变量来大大增加线性规划的灵活性有了更全面的理解。这篇文章旨在介绍辅助变量，并给出了一些它们使用的例子。它们还有其他的使用方法，你可以进一步探索。一开始它们可能有点难以理解，但一旦你掌握了它们，一个全新的线性规划优化可能性世界就会打开！
